# Copy to .env and fill in to enable LIVE AI analysis.
# Without a key, Drift runs in a deterministic "demo engine" mode that still works end-to-end.
ANTHROPIC_API_KEY=
# Optional overrides:
# DRIFT_MODEL=claude-sonnet-5
# PORT=3000
